# Core dependencies — works on Python 3.10 through 3.14.
# Voice in/out works with this set thanks to the sounddevice fallback,
# even without PyAudio (whose wheels lag behind Python releases).
#
#   pip install -r requirements-core.txt

# --- TTS (offline, ships with Windows SAPI5 voices) ---
pyttsx3>=2.98

# --- STT capture + recogniser ---
SpeechRecognition>=3.10.4
sounddevice>=0.5.0           # PortAudio bindings — Python 3.14 wheels available
numpy>=1.26                  # Required by sounddevice for sample arrays

# --- LLM brain + .env support ---
python-dotenv>=1.0.1         # Loads .env into os.environ
requests>=2.32.0             # Talks to OpenAI-compatible chat endpoints
google-genai>=0.3.0          # Native Google Gemini SDK (Google AI Studio keys)

# --- GUI (modern PySide6 dashboard; legacy Tkinter ships with Python) ---
PySide6>=6.7.0               # Qt for Python — adjustable splitter, modern theme

# --- Misc utilities ---
psutil>=6.0.0                # CPU / memory / process info
python-dateutil>=2.9.0
colorama>=0.4.6              # Coloured console logs on Windows
Pillow>=10.0.0               # Screenshot capture (ImageGrab) + tray icon glyph
pystray>=0.19.5              # System-tray icon (Tk fallback only — Qt has its own)

# --- Packaging ---
pyinstaller>=6.10.0          # For building JARVIS.exe
